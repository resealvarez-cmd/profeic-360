import React from 'react';

export const dynamic = 'force-dynamic';

export default function AcompanamientoLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return <>{children}</>;
}
